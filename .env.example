# CommitmentVault contract address on Arc Testnet (set after deploy)
# Get this address after running: forge script scripts/Deploy.s.sol --rpc-url https://rpc.testnet.arc.network --broadcast
NEXT_PUBLIC_COMMITMENT_VAULT_ADDRESS=

# For Foundry deploy (scripts/Deploy.s.sol)
# PRIVATE_KEY=your_private_key_here
