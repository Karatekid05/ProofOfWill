# CommitmentFactory contract address on Arc Testnet (set after deploy)
NEXT_PUBLIC_COMMITMENT_FACTORY_ADDRESS=

# CommitmentVault contract address (fallback if factory not used)
NEXT_PUBLIC_COMMITMENT_VAULT_ADDRESS=

# DualConsensusVault or V2 (two-party agreements). Prefer V2 for recovery + fee.
NEXT_PUBLIC_DUAL_CONSENSUS_VAULT_ADDRESS=

# Deploy (run locally; never commit PRIVATE_KEY):
#   export PRIVATE_KEY=0x...   # your wallet that will pay gas
#   export OWNER=0x...         # optional: for V2 recovery + fee
#   forge script scripts/Deploy.s.sol --rpc-url https://rpc.testnet.arc.network --broadcast --private-key $PRIVATE_KEY
# Deploy only V2 (keep existing vault/factory): export OWNER=0x... && forge script scripts/DeployV2Only.s.sol --rpc-url https://rpc.testnet.arc.network --broadcast --private-key $PRIVATE_KEY
# Then set NEXT_PUBLIC_DUAL_CONSENSUS_VAULT_ADDRESS to the V2 address.
